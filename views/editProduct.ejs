<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="/styles/profile.css" rel="stylesheet"> 

</head>
<body >
    <nav >
        <ul>
            <li><a class="link-body-emphasis" href="/">Home</a></li>
         </ul>
    </nav>
    <div class="container themed-container text-center" style="margin-top: 10px;">
       <%= post.image_url %>
           <% if (pro) { %>
            <div class="product-container d-flex flex-column flex-lg-row">
            
              <form id="productForm" action="/editProduct/<%= pro.id %>" method="POST" enctype="multipart/form-data" class="d-flex flex-column flex-lg-row w-100">
            
                <!-- Product Image Upload and Preview (Left Side) -->
                <div class="product-image col-12 col-lg-6 mb-4 mb-lg-0 d-flex flex-column justify-content-center align-items-center">
                    <!-- Image preview area -->
                    <div id="imagePreviewContainer" class="mb-3">
                        <img id="imagePreview" src="<%= pro.image %>" alt="Product Image Preview" class="img-fluid">
                    </div>
            
                    <!-- Upload button -->
                    <label for="imageUpload" class="upload-button btn btn-outline-secondary">
                        <i class="fas fa-camera"></i> Upload Image
                    </label>
                    <input type="file" name="image" id="imageUpload" accept="image/*" style="display: none;" onchange="previewImage(event)">
                </div>
            
                <!-- Product Details Form (Right Side) -->
                <div class="product-details col-12 col-lg-6 d-flex flex-column justify-content-center">
                    <h1 class="product-title mb-4">Edit Product</h1>
                    
                    <div class="form-group">
                        <label for="productName">Product Name:</label>
                        <input type="text" id="productName" name="name" value="<%= pro.productname %>" placeholder="<%= pro.productname %>" class="form-control" required>
                    </div>
            
                    <div class="form-group mt-3">
                        <label for="price">Price:</label>
                        <input type="number" id="price" name="price" class="form-control" value="<%= pro.price %>" placeholder="<%= pro.price %>" step="0.01" required>
                    </div>
            
                    <div class="form-group mt-3">
                        <label for="description">Description:</label>
                        <textarea id="description" name="description" class="form-control" value="<%= pro.description %>" placeholder="<%= pro.description %>" rows="6" required><%= pro.description %></textarea>
                    </div>
            
                    <!-- Submit Button -->
                    <button type="submit" class="order-button btn btn-primary mt-4 w-100">Edit Product</button>
                </div>
                 
            </form>
            
        </div>
        <form id="productForm" action="/deleteProduct/<%= pro.id %>" method="POST" enctype="multipart/form-data" class="d-flex flex-column flex-lg-row w-100">
            <button type="submit" class="order-button btn btn-primary mt-4 w-100" style="background-color: rgb(255, 9, 9);">Delete Product</button>

        </form>
            <% } else { %>
                <p>Not found</p>
             <% } %>
    </div>
   
      
  

<script>
    function previewImage(event) {
        const output = document.getElementById('imagePreview');
        output.src = URL.createObjectURL(event.target.files[0]);
        output.style.display = 'block';
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>

