<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Detail</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/styles/profile.css" rel="stylesheet"> 

   <style>
        /* Playful and Vibrant Theme */
        body {
            font-family: 'Poppins', sans-serif;
            color: #2C3E50;
        }
        .product-container {
            background-color: #FFF;
            border-radius: 15px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin: 50px auto;
            max-width: 900px;
        }
        .product-image {
            border-radius: 15px;
            background-color: #E74C3C;
            padding: 15px;
            text-align: center;
        }
        .product-image img {
            max-width: 100%;
            border-radius: 10px;
        }
        .product-details h1 {
            color: #E67E22;
            font-weight: 700;
            margin-bottom: 20px;
        }
        .product-title {
            font-size: 20px;
            font-weight: 600;
            color: #34495E;
        }
        .price-title {
            font-size: 20px;
            font-weight: 600;
            color: #34495E;
            margin-top: 10px;
        }
        .price {
            font-size: 24px;
            color: #1ABC9C;
            font-weight: 600;
        }
        .description {
            margin-top: 20px;
            font-size: 16px;
            color: #34495E;
        }
        .add-to-cart-btn {
            background-color: #F1C40F;
            color: #2C3E50;
            font-weight: 700;
            border-radius: 30px;
            padding: 12px 30px;
            border: none;
            margin-top: 30px;
            text-transform: uppercase;
        }
        .add-to-cart-btn:hover {
            background-color: #F39C12;
            color: #FFF;
        }
        .product-info {
            margin-top: 40px;
        }
        .product-info h3 {
            color: #E67E22;
            font-weight: 600;
        }
        .product-info ul {
            list-style: none;
            padding: 0;
        }
        .product-info ul li {
            background-color: #F8C471;
            padding: 10px;
            border-radius: 10px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a class="link-body-emphasis" href="/">Home</a></li>
        </ul>
    </nav>
    <div class="container themed-container" style="margin-top: 10px;">
      <% if (pro) { %>
      <div class="container product-container">
        <div class="row">
            <div class="col-md-6 product-image text-center">
                <img src="<%= pro.image %>" alt="Product Image">
            </div>
            <div class="col-md-6 product-details text-start">
                <p class="product-title">Product Name</p>
                <h1><%= pro.productname %></h1>
                
                <p class="price-title">Price</p>
                <p class="price"><%= pro.price %> Rs.</p>
                
                <form action="/add-cart/<%= pro.id %>" method="post">
                    <button class="add-to-cart-btn">
                        Add to Cart
                    </button>
                </form>
            </div>
        </div>
        <div class="row product-info">
            <div class="col-md-12 text-start">
                <h3>Product Information</h3>
                <ul>
                    <li><%= pro.description %></li>
                </ul>
            </div>
        </div>
      </div>
      <% } else { %>
        <p>Not found</p>
      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
